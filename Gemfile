source 'https://rubygems.org'

gem 'bundler'
gem 'rake'
gem 'hanami',       '~> 0.8'
gem 'hanami-model', '~> 0.6'

# API
gem 'roar'
gem 'multi_json'

gem 'sqlite3'

gem 'sass'

group :development do
  # Code reloading
  gem 'shotgun'
end

group :test, :development do
  gem 'dotenv', '~> 2.0'

  gem 'byebug'
  gem 'pry'
end

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :production do
  # gem 'puma'
end
